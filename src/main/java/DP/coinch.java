package DP;

public class coinch {
    public static void main(String[] args) {

    }
    /*
    fn(int[] coins, int amt,int ind,int sum,int elem)
    {
        if(ind<0){
            return 0;
        }
        if(sum==target)return elem;
        int pick = f(coins,amt,ind-1,sum+coins[ind],elem+1);
        int nopick = f(coins,amt,ind-1,sum,elem);;
        return min(pick,nopick)
    }
     */
    public int coinChange(int[] coins, int amount) {


        return 0;

    }

}
